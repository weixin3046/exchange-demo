import { PlaceholderExamples } from "@/components/Icons/PlaceholderExamples";

export default function PlaceholderDemoPage() {
  return (
    <div className="min-h-screen bg-black">
      <PlaceholderExamples />
    </div>
  );
}
